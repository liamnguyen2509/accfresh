import React from "react";

import Layout from "../../../components/Layout/Layout";

const Home = (props) => {
    return (
        <Layout>
            
        </Layout>
    ); 
}

export default Home;